<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('../partials/_meta') %>
  <title>Blog</title>
</head>

<body>
  <div class="container-fluid">
    <%- include('../partials/_header') %>
    <!--
    <%- include('../partials/_sideBar') %>
    <div class="container">
      <h1 class="blog-header">Blog Articles</h1>
      <a href="/articles/new" class="btn btn-success">New Article</a>

      <!--
        articles.forEach(article => {
          // output html to wrap an article in a nice group of DIVs
          // and output the title, date, and link
          <h4><%= article.title %></h4>
        })
      -->

    <% articles.forEach(article=> { // where articles is an array of articles passed from the route handler %>
    <%- include('../partials/_articleCard', { article }) %>
    <% }) %>
  </div>
  <a href='/join'>To the join page </a>
  <%- include('../partials/_footer') %>
  -->
  </div>
</body>

</html>